/**
 * Dias ISO 1=seg … 7=dom.
 * Padrão = todos os dias: o profissional desmarca folgas em Configurações (nada impede sábado/dom se atender).
 */
export const DEFAULT_WORK_WEEKDAYS_ALL: readonly number[] = [1, 2, 3, 4, 5, 6, 7];
